let sudoku;

function setup() {
	createCanvas(400, 400);
	sudoku = new Sudoku();
}

function draw() {
	background(255);
	sudoku.show();

	// solve

	// if board is valid find the first 0 and increase to 1
	// go through all cells
	//
	// val
}
